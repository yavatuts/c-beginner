#include <stdio.h>
int main() {
    // دنش‌آموزی میخواهد معدل ۵ نمره خود را حساب کند
    int a,b,c,d,e;
    scanf("%i,%i,%i,%i,%i", &a, &b, &c, &d, &e);
    printf("The Average is : %f", (a+b+c+d+e)/5.0);
    return 0;
}
